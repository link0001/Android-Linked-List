package com.example.myfirstapp;

public interface DoAction {

    // This is just a regular method so it can return something or
    // take arguments if you like.
    public void doAction(Object data, boolean isLastElement);
}
